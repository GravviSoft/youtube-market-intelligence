version: "3.9"

networks:
  proxy-net:
    external: true

services:
  youtube-intel-frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3001:3000"
    expose:
      - "3000"
    networks:
      - proxy-net
    restart: unless-stopped
